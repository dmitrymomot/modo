[package]
name = "upload"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
modo = { path = "../../modo" }
modo-upload = { path = "../../modo-upload" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
